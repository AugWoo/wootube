extends base.pug
include mixins/video

block content
  div.video-grid
    each item in videos
      +video(item)
      +video(item)
      +video(item)
      +video(item)
      +video(item)
      +video(item)
      +video(item)
    else
      span.empty__message No videos found